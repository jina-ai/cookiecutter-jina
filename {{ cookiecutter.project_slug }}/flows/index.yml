!Flow
pods:
  encoder:
    uses: pods/encode.yml
    parallel: $JINA_PARALLEL
    timeout_ready: 600000
    read_only: true
  chunk_indexer:
    uses: pods/chunk.yml
    shards: $JINA_SHARDS
    uses_before: pods/craft.yml
    separated_workspace: true
