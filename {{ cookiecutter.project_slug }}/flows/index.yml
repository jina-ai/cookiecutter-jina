!Flow
with:
  logserver: true
pods:
  crafter:
    uses_internal: pods/craft.yml
    read_only: true
  encoder:
    uses_internal: pods/encode.yml
    parallel: $PARALLEL
    timeout_ready: 600000
    read_only: true
  chunk_idx:
    uses_internal: pods/chunk.yml
    shards: $SHARDS
    separated_workspace: true
  doc_idx:
    uses_internal: pods/doc.yml
    needs: gateway
  join_all:
    uses_internal: _merge
    needs: [doc_idx, chunk_idx]
